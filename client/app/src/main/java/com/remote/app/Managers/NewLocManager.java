package com.remote.app.Managers;

import android.location.Location;
import android.location.LocationListener;

public class NewLocManager extends Location {
    public NewLocManager(String provider) {
        super(provider);
    }
}
